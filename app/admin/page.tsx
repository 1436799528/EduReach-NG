import { createClient } from '@/lib/supabase/server'
import Link from 'next/link'
export default async function AdminPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();const {data:profile}=await supabase.from('profiles').select('role,full_name').eq('id',user!.id).maybeSingle();if(!profile||!['contributor','moderator','admin','super_admin'].includes(profile.role))return <div className="card"><div className="eyebrow">ADMIN</div><h1>Access denied</h1></div>;const [{count:users},{count:pending},{count:resources},{count:templates}]=await Promise.all([supabase.from('profiles').select('*',{count:'exact',head:true}),supabase.from('edureach_announcements').select('*',{count:'exact',head:true}).eq('verification_status','pending'),supabase.from('resources').select('*',{count:'exact',head:true}).eq('status','pending'),supabase.from('letter_templates').select('*',{count:'exact',head:true})]);return <div><section style={{background:'var(--primary-navy)',color:'#fff',borderRadius:18,padding:'28px 30px',marginBottom:22}}><div className="eyebrow" style={{color:'#ffd2b9'}}>ADMIN OPERATIONS</div><h1 style={{color:'#fff',margin:'8px 0'}}>Manage EduReach.</h1><p style={{margin:0,color:'#e8eef9'}}>Welcome{profile.full_name?`, ${profile.full_name}`:''}.</p></section><div className="grid grid-3"><div className="card"><div className="label">Students</div><div className="stat">{users??0}</div></div><div className="card"><div className="label">Pending updates</div><div className="stat">{pending??0}</div></div><div className="card"><div className="label">Letter templates</div><div className="stat">{templates??0}</div></div></div><section className="section"><div className="grid grid-3"><Link className="service-card-container" href="/admin/templates"><div className="service-icon-badge" style={{background:'var(--secondary-orange)'}}>✍</div><h3 className="service-title" style={{marginTop:12,textTransform:'none'}}>Letter templates</h3><p className="service-description">Add, enable or disable letters.</p></Link><Link className="service-card-container" href="/app/updates"><div className="service-icon-badge">!</div><h3 className="service-title" style={{marginTop:12,textTransform:'none'}}>School updates</h3><p className="service-description">Review public notices.</p></Link><Link className="service-card-container" href="/app/resources"><div className="service-icon-badge">↗</div><h3 className="service-title" style={{marginTop:12,textTransform:'none'}}>Resources</h3><p className="service-description">Review approved materials.</p></Link></div></section></div>}
