'use client'
import {useEffect,useState} from 'react'
import {createClient} from '@/lib/supabase/client'
export default function AdminResources(){const db=createClient();const [rows,setRows]=useState<any[]>([]);const [form,setForm]=useState({title:'',description:'',resource_type:'official-link',external_url:''});const [msg,setMsg]=useState('');
 async function load(){const {data}=await db.from('resources').select('*').order('created_at',{ascending:false}).limit(50);setRows(data||[])}useEffect(()=>{load()},[])
 async function add(e:React.FormEvent){e.preventDefault();const {data:{user}}=await db.auth.getUser();const {error}=await db.from('resources').insert({title:form.title.trim(),description:form.description.trim()||null,resource_type:form.resource_type,external_url:form.external_url.trim()||null,uploaded_by:user?.id,status:'approved'});setMsg(error?error.message:'Resource added.');if(!error){setForm({title:'',description:'',resource_type:'official-link',external_url:''});load()}}
 async function setStatus(id:string,status:string){const {error}=await db.from('resources').update({status,updated_at:new Date().toISOString()}).eq('id',id);if(error)setMsg(error.message);else load()}
 return <div><div className="page-head"><div><div className="eyebrow">ADMIN · RESOURCES</div><h1>Resources</h1></div></div><div className="grid grid-2"><form className="service-card-container form" onSubmit={add}><h2 style={{margin:0}}>Add resource</h2><div className="field"><label>Title</label><input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="field"><label>Description</label><input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div className="grid grid-2"><div className="field"><label>Type</label><select value={form.resource_type} onChange={e=>setForm({...form,resource_type:e.target.value})}><option>official-link</option><option>guide</option><option>form</option><option>past-question</option><option>handout</option><option>notice</option></select></div><div className="field"><label>URL</label><input type="url" required value={form.external_url} onChange={e=>setForm({...form,external_url:e.target.value})}/></div></div>{msg&&<div className="badge">{msg}</div>}<button className="btn btn-primary">Add resource</button></form><div className="service-card-container"><div className="page-head"><h2 style={{margin:0}}>Library</h2><span className="badge">{rows.length}</span></div><div className="list">{rows.map(r=><div className="list-item" key={r.id}><div style={{display:'flex',justifyContent:'space-between',gap:10}}><strong>{r.title}</strong><span className={`badge ${r.status==='approved'?'good':r.status==='pending'?'warn':'bad'}`}>{r.status}</span></div><div className="muted" style={{fontSize:12,marginTop:4}}>{r.resource_type}</div><div className="actions" style={{marginTop:8}}><button className="btn btn-secondary" onClick={()=>setStatus(r.id,'approved')}>Approve</button><button className="btn btn-ghost" onClick={()=>setStatus(r.id,'rejected')}>Reject</button></div></div>)}</div></div></div></div>}
