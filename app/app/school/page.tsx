import { createClient } from '@/lib/supabase/server'
export default async function SchoolPage(){const supabase=await createClient();const {data:school}=await supabase.from('institutions').select('*').eq('acronym','UNICAL').maybeSingle();return <><div className="page-head"><div><div className="eyebrow">MY SCHOOL</div><h1>{school?.school_name||'University of Calabar'}</h1><p className="muted">Your institutional home. Data is populated only when a source is available.</p></div></div><div className="grid grid-2"><div className="card"><h2>Institution profile</h2><p><strong>Type:</strong> {school?.institution_type||'University'}</p><p><strong>State:</strong> {school?.state||'Cross River'}</p><p><strong>Verified:</strong> {school?.is_verified?'Yes':'No'}</p></div><div className="card"><h2>Official links</h2><div className="list">{[['Website',school?.website_url],['Admission portal',school?.admission_portal_url],['Student portal',school?.student_portal_url]].map(([n,u])=><a key={n} className="list-item" href={u||'#'} target="_blank" rel="noreferrer"><strong>{n}</strong><div className="muted">{u||'Not available'}</div></a>)}</div></div></div></>}
