'use client'
import { useMemo,useState } from 'react'
import { calculateGpa,calculateRequiredGpa,targetStatus } from '@/lib/calculations'
export default function GpaPage(){const [rows,setRows]=useState([{units:3,point:5},{units:3,point:4}]);const [current,setCurrent]=useState(3.05);const [completed,setCompleted]=useState(90);const [remaining,setRemaining]=useState(45);const [target,setTarget]=useState(3.5);const gpa=useMemo(()=>calculateGpa(rows),[rows]);const req=calculateRequiredGpa(current,completed,remaining,target);const status=req===null?null:targetStatus(req,5);function update(i:number,k:'units'|'point',v:number){setRows(r=>r.map((x,n)=>n===i?{...x,[k]:v}:x))}return <><div className="page-head"><div><div className="eyebrow">CALCULATE</div><h1>GPA & CGPA tools</h1></div></div><div className="grid grid-2"><div className="card"><h2>Semester GPA</h2><p className="muted">Enter each course's credit units and grade point.</p>{rows.map((r,i)=><div className="grid grid-2" key={i} style={{marginBottom:10}}><div className="field"><label>Units</label><input type="number" min="1" value={r.units} onChange={e=>update(i,'units',Number(e.target.value))}/></div><div className="field"><label>Grade point</label><input type="number" min="0" max="5" step="0.01" value={r.point} onChange={e=>update(i,'point',Number(e.target.value))}/></div></div>)}<button className="btn btn-secondary" onClick={()=>setRows([...rows,{units:2,point:3}])}>Add course</button><div style={{marginTop:22}}><div className="label">Calculated GPA</div><div className="stat">{gpa.toFixed(2)}</div></div></div><div className="card"><h2>Can I still reach my target?</h2><div className="form"><div className="field"><label>Current CGPA</label><input type="number" step="0.01" value={current} onChange={e=>setCurrent(Number(e.target.value))}/></div><div className="field"><label>Completed credits</label><input type="number" value={completed} onChange={e=>setCompleted(Number(e.target.value))}/></div><div className="field"><label>Remaining credits</label><input type="number" value={remaining} onChange={e=>setRemaining(Number(e.target.value))}/></div><div className="field"><label>Target CGPA</label><input type="number" step="0.01" value={target} onChange={e=>setTarget(Number(e.target.value))}/></div></div>{req!==null&&<div style={{marginTop:20}}><div className="label">Required average GPA</div><div className="stat">{req.toFixed(2)}</div><span className={`badge ${status?.tone==='good'?'good':'bad'}`}>{status?.label}</span>{req>5&&<p className="muted">The target exceeds the maximum 5-point average for the remaining credits.</p>}</div>}</div></div></>}
