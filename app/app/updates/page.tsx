import Link from 'next/link'
import { createClient } from '@/lib/supabase/server'
export default async function UpdatesPage(){const supabase=await createClient();const {data:updates}=await supabase.from('edureach_announcements').select('id,title,summary,body,category,priority,published_at,last_verified_at,source_url').eq('verification_status','verified').not('published_at','is',null).order('published_at',{ascending:false});return <div><section className="service-card-container" style={{background:'var(--primary-navy)',color:'#fff',border:0,marginBottom:20}}><div className="eyebrow" style={{color:'#ffd2b9'}}>NOTICE BOARD</div><h1 style={{color:'#fff',margin:'8px 0'}}>What is happening at school?</h1></section><div style={{display:'grid',gap:14}}>{updates?.map(u=><article className="service-card-container" key={u.id}><div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'flex-start',flexWrap:'wrap'}}><div><span className={`badge ${u.priority==='urgent'?'bad':u.priority==='important'?'warn':'good'}`}>{u.priority==='urgent'?'URGENT':u.priority==='important'?'IMPORTANT':'GENERAL'}</span><div className="eyebrow" style={{marginTop:10}}>{(u.category||'notice').replaceAll('_',' ')}</div><h2 style={{color:'var(--primary-navy)',margin:'4px 0 8px',fontSize:21}}>{u.title}</h2></div><Link href={`/app/updates/${u.id}`} className="btn btn-ghost">Read</Link></div><p style={{lineHeight:1.55,marginBottom:10}}>{u.summary}</p><div className="muted" style={{fontSize:12}}>Published {u.published_at?new Date(u.published_at).toLocaleDateString('en-NG',{day:'numeric',month:'short',year:'numeric'}):'—'} · Verified {u.last_verified_at?new Date(u.last_verified_at).toLocaleDateString('en-NG',{day:'numeric',month:'short',year:'numeric'}):'—'}</div>{u.source_url&&<a href={u.source_url} target="_blank" rel="noreferrer" className="btn btn-secondary" style={{marginTop:12}}>Official source ↗</a>}</article>)}</div>{!updates?.length&&<div className="service-card-container"><h2>No verified notices yet.</h2></div>}</div>}
