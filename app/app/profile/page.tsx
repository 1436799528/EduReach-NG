'use client'
import { useEffect,useState } from 'react'
import { createClient } from '@/lib/supabase/client'
export default function ProfilePage(){const [form,setForm]=useState({full_name:'',school:'',level:'',department:''});const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){setLoading(false);return}setEmail(user.email||'');const {data}=await s.from('profiles').select('full_name,school,level,department').eq('id',user.id).maybeSingle();if(data)setForm({full_name:data.full_name||'',school:data.school||'',level:data.level||'',department:data.department||''});setLoading(false)})()},[]);async function save(e:React.FormEvent){e.preventDefault();setMessage('');const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)return;const {error}=await s.from('profiles').update(form).eq('id',user.id);setMessage(error?'We could not save your details. Please try again.':'Your student details have been saved.')}if(loading)return <div className="card">Loading your student profile…</div>;return <div><section style={{background:'#102a56',color:'#fff',borderRadius:22,padding:'30px',marginBottom:22}}><div className="eyebrow" style={{color:'#bcd1ff'}}>MY PROFILE</div><h1 style={{color:'#fff',margin:'8px 0'}}>Your student details</h1><p style={{margin:0,color:'#dbe7ff',lineHeight:1.6,maxWidth:700}}>Keep your school, level and department current so EduReach can show you more relevant information.</p></section><div className="grid grid-2"><form className="card form" onSubmit={save}><div className="eyebrow">STUDENT INFORMATION</div><div className="field"><label>Full name</label><input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})} placeholder="Your full name"/></div><div className="field"><label>Email address</label><input value={email} disabled/></div><div className="field"><label>University / Polytechnic / College</label><input value={form.school} onChange={e=>setForm({...form,school:e.target.value})} placeholder="University of Calabar"/></div><div className="grid grid-2"><div className="field"><label>Level</label><input value={form.level} onChange={e=>setForm({...form,level:e.target.value})} placeholder="300 Level"/></div><div className="field"><label>Department</label><input value={form.department} onChange={e=>setForm({...form,department:e.target.value})} placeholder="Electrical Engineering"/></div></div>{message&&<div className="notice">{message}</div>}<button className="btn btn-primary">Save student details</button></form><div className="card"><div className="eyebrow">WHAT THIS CHANGES</div><h2 style={{fontSize:20}}>Make EduReach fit your school life</h2><div className="list"><div className="list-item"><strong>My School</strong><div className="muted">Show information for your institution first.</div></div><div className="list-item"><strong>Updates</strong><div className="muted">Prioritize relevant notices when your school data is available.</div></div><div className="list-item"><strong>Academic tools</strong><div className="muted">Use your level and programme context when planning calculations.</div></div></div><div className="notice" style={{marginTop:14}}><strong>Privacy:</strong><div className="muted" style={{marginTop:5}}>Your profile is private. EduReach will not expose your email, phone number, matric number or private documents through public search.</div></div></div></div></div>}