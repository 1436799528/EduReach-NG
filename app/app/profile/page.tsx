'use client'
import { useEffect,useState } from 'react'
import { createClient } from '@/lib/supabase/client'
export default function ProfilePage(){const [form,setForm]=useState({full_name:'',school:'',level:'',department:''});const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){setLoading(false);return}setEmail(user.email||'');const {data}=await s.from('profiles').select('full_name,school,level,department').eq('id',user.id).maybeSingle();if(data)setForm({full_name:data.full_name||'',school:data.school||'',level:data.level||'',department:data.department||''});setLoading(false)})()},[]);async function save(e:React.FormEvent){e.preventDefault();setMessage('');const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user)return;const {error}=await s.from('profiles').update(form).eq('id',user.id);setMessage(error?'We could not save your profile. Please try again.':'Profile saved.')}if(loading)return <div className="card">Loading your profile…</div>;return <div><section style={{background:'#102a56',color:'#fff',borderRadius:22,padding:'28px 30px',marginBottom:22}}><div className="eyebrow" style={{color:'#bcd1ff'}}>PROFILE & SETTINGS</div><h1 style={{color:'#fff',margin:'8px 0'}}>Keep your student details current.</h1><p style={{margin:0,color:'#dbe7ff',lineHeight:1.6}}>EduReach uses these details to personalize your school experience. You can change them whenever they change.</p></section><div className="grid grid-2"><form className="card form" onSubmit={save}><div className="eyebrow">YOUR DETAILS</div><div className="field"><label>Full name</label><input value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></div><div className="field"><label>Email</label><input value={email} disabled/></div><div className="field"><label>School</label><input value={form.school} onChange={e=>setForm({...form,school:e.target.value})} placeholder="University of Calabar"/></div><div className="grid grid-2"><div className="field"><label>Level</label><input value={form.level} onChange={e=>setForm({...form,level:e.target.value})} placeholder="300"/></div><div className="field"><label>Department</label><input value={form.department} onChange={e=>setForm({...form,department:e.target.value})} placeholder="Electrical Engineering"/></div></div>{message&&<div className="notice">{message}</div>}<button className="btn btn-primary">Save changes</button></form><div className="card"><div className="eyebrow">PRIVACY</div><h2 style={{fontSize:20}}>Your information</h2><p className="muted" style={{lineHeight:1.6}}>Your profile is private to your account. EduReach should never expose your email, phone number, matric number or private documents through public search.</p><div className="notice"><strong>Good practice</strong><div className="muted" style={{marginTop:5}}>Only enter information needed to personalize your student experience.</div></div></div></div></div>}
