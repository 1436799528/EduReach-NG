'use client'
import { useState } from 'react'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/client'
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const supabase=createClient();const {error}=await supabase.auth.signInWithPassword({email,password});if(error)setError('Email or password is incorrect. Please check your details and try again.');else window.location.href='/app';setLoading(false)}return <main style={{minHeight:'100vh',display:'grid',gridTemplateColumns:'minmax(0,1fr) minmax(360px,460px)'}}><section style={{background:'#102a56',color:'#fff',padding:'clamp(30px,7vw,80px)',display:'flex',flexDirection:'column',justifyContent:'space-between'}}><Link href="/" className="logo" style={{color:'#fff'}}>EduReach <span>Hub</span></Link><div style={{maxWidth:620}}><div className="eyebrow" style={{color:'#bcd1ff'}}>YOUR SCHOOL FRONT DESK</div><h1 style={{fontSize:'clamp(38px,5vw,62px)',color:'#fff',lineHeight:1.05}}>Your school information, when you need it.</h1><p style={{color:'#dbe7ff',lineHeight:1.7,fontSize:17}}>Sign in to see your school, notices, tools, letters and saved student activity.</p><div style={{display:'flex',gap:8,flexWrap:'wrap',marginTop:20}}><span className="badge">JAMB</span><span className="badge">School notices</span><span className="badge">GPA & CGPA</span></div></div><div style={{color:'#b9c9e5',fontSize:13}}>Independent student-support service.</div></section><section style={{padding:'clamp(24px,6vw,60px)',display:'flex',alignItems:'center'}}><div style={{width:'100%',maxWidth:390}}><Link href="/" className="logo">EduReach <span>Hub</span></Link><div style={{marginTop:38}}><div className="eyebrow">SIGN IN</div><h2 style={{fontSize:30,margin:'7px 0'}}>Welcome back</h2><p className="muted">Continue with your EduReach account.</p><form className="form" onSubmit={submit} style={{marginTop:24}}><div className="field"><label>Email address</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" placeholder="you@example.com"/></div><div className="field"><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></div>{error&&<div className="badge bad">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><p className="muted" style={{marginTop:18}}>New to EduReach? <Link href="/signup" style={{color:'var(--brand)',fontWeight:700}}>Create your student account</Link></p></div></div></section></main>}
