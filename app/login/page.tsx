'use client'
import {useState} from 'react'
import Link from 'next/link'
import {createClient} from '@/lib/supabase/client'
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError('Wrong email or password.');else window.location.href='/app';setLoading(false)}return <main style={{minHeight:'100vh',display:'grid',gridTemplateColumns:'minmax(0,1fr) minmax(360px,460px)'}}><section style={{background:'var(--primary-navy)',color:'#fff',padding:'clamp(28px,7vw,78px)',display:'flex',flexDirection:'column',justifyContent:'space-between'}}><Link href="/" className="logo" style={{color:'#fff'}}>EduReach <span>Hub</span></Link><div style={{maxWidth:620}}><div className="eyebrow" style={{color:'#ffd2b9'}}>WELCOME BACK</div><h1 style={{fontSize:'clamp(38px,5vw,62px)',color:'#fff',lineHeight:1.03}}>Your school desk is waiting.</h1><div style={{display:'flex',gap:9,flexWrap:'wrap',marginTop:20}}><span className="badge">JAMB</span><span className="badge">UNICAL</span><span className="badge">GPA</span><span className="badge">Letters</span></div></div><div style={{color:'#c8d4e8',fontSize:12}}>EduReach Hub</div></section><section style={{padding:'clamp(24px,6vw,60px)',display:'flex',alignItems:'center'}}><div style={{width:'100%',maxWidth:390}}><Link href="/" className="logo">EduReach <span>Hub</span></Link><div style={{marginTop:34}}><div className="eyebrow">SIGN IN</div><h2 style={{fontSize:30,margin:'7px 0'}}>Continue</h2><form className="form" onSubmit={submit} style={{marginTop:22}}><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></div><div className="field"><label>Password</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/></div>{error&&<div className="badge bad">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form><p className="muted" style={{marginTop:16}}>No account? <Link href="/signup" style={{color:'var(--accent-light-blue)',fontWeight:800}}>Create one</Link></p></div></div></section></main>}
