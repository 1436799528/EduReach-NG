'use client'
import {useState} from 'react'
import Link from 'next/link'
import {createClient} from '@/lib/supabase/client'
export default function SignupPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [error,setError]=useState('');const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const {data,error}=await createClient().auth.signUp({email,password,options:{data:{full_name:name}}});if(error)setError(error.message||'Account could not be created.');else if(data.user)setDone(true);setLoading(false)}return <main style={{minHeight:'100vh',display:'grid',gridTemplateColumns:'minmax(0,1fr) minmax(360px,460px)'}}><section style={{background:'var(--primary-navy)',color:'#fff',padding:'clamp(28px,7vw,78px)',display:'flex',flexDirection:'column',justifyContent:'space-between'}}><Link href="/" className="logo" style={{color:'#fff'}}>EduReach <span>Hub</span></Link><div style={{maxWidth:620}}><div className="eyebrow" style={{color:'#ffd2b9'}}>CREATE ACCOUNT</div><h1 style={{fontSize:'clamp(38px,5vw,62px)',color:'#fff',lineHeight:1.03}}>Start using your school desk.</h1><div style={{display:'flex',gap:9,flexWrap:'wrap',marginTop:20}}><span className="badge">My School</span><span className="badge">JAMB</span><span className="badge">Letters</span><span className="badge">Calculators</span></div></div><div style={{color:'#c8d4e8',fontSize:12}}>EduReach Hub</div></section><section style={{padding:'clamp(24px,6vw,60px)',display:'flex',alignItems:'center'}}><div style={{width:'100%',maxWidth:390}}><Link href="/" className="logo">EduReach <span>Hub</span></Link><div style={{marginTop:34}}><div className="eyebrow">ACCOUNT</div><h2 style={{fontSize:30,margin:'7px 0'}}>Create account</h2>{done?<div className="notice" style={{marginTop:22}}><strong>Account created.</strong><div className="muted" style={{marginTop:5}}>Check your email if verification is enabled, then <Link href="/login" style={{color:'var(--accent-light-blue)',fontWeight:800}}>sign in</Link>.</div></div>:<form className="form" onSubmit={submit} style={{marginTop:22}}><div className="field"><label>Full name</label><input required value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></div><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></div><div className="field"><label>Password</label><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/><small className="muted">8 characters minimum</small></div>{error&&<div className="badge bad">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?'Creating…':'Create account'}</button></form>}<p className="muted" style={{marginTop:16}}>Already registered? <Link href="/login" style={{color:'var(--accent-light-blue)',fontWeight:800}}>Sign in</Link></p></div></div></section></main>}
