'use client'
import { useState } from 'react'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/client'
export default function SignupPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [error,setError]=useState('');const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const supabase=createClient();const {data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});if(error)setError('We could not create your account. Check your details and try again.');else if(data.user)setDone(true);setLoading(false)}return <main style={{minHeight:'100vh',display:'grid',gridTemplateColumns:'minmax(0,1fr) minmax(360px,460px)'}}><section style={{background:'#102a56',color:'#fff',padding:'clamp(30px,7vw,80px)',display:'flex',flexDirection:'column',justifyContent:'space-between'}}><Link href="/" className="logo" style={{color:'#fff'}}>EduReach <span>Hub</span></Link><div style={{maxWidth:620}}><div className="eyebrow" style={{color:'#bcd1ff'}}>START WITH THE PROBLEM</div><h1 style={{fontSize:'clamp(38px,5vw,62px)',color:'#fff',lineHeight:1.05}}>Your school front desk, in your pocket.</h1><p style={{color:'#dbe7ff',lineHeight:1.7,fontSize:17}}>Create a free account and personalize EduReach around your institution.</p></div><div style={{color:'#b9c9e5',fontSize:13}}>You can complete your school profile later.</div></section><section style={{padding:'clamp(24px,6vw,60px)',display:'flex',alignItems:'center'}}><div style={{width:'100%',maxWidth:390}}><Link href="/" className="logo">EduReach <span>Hub</span></Link><div style={{marginTop:38}}><div className="eyebrow">CREATE ACCOUNT</div><h2 style={{fontSize:30,margin:'7px 0'}}>Get started</h2><p className="muted">Only the basics are required right now.</p>{done?<div className="notice" style={{marginTop:24}}><strong>Account created.</strong><div className="muted" style={{marginTop:5}}>Check your email if verification is enabled, then <Link href="/login" style={{color:'var(--brand)',fontWeight:700}}>sign in</Link>.</div></div>:<form className="form" onSubmit={submit} style={{marginTop:24}}><div className="field"><label>Full name</label><input required value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></div><div className="field"><label>Email address</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></div><div className="field"><label>Password</label><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/><small className="muted">Use at least 8 characters.</small></div>{error&&<div className="badge bad">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?'Creating…':'Create account'}</button></form>}<p className="muted" style={{marginTop:18}}>Already registered? <Link href="/login" style={{color:'var(--brand)',fontWeight:700}}>Sign in</Link></p></div></div></section></main>}
