'use client'
import { useState } from 'react'
import Link from 'next/link'
import { createClient } from '@/lib/supabase/client'

export default function SignupPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [error,setError]=useState('');const [done,setDone]=useState(false);const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const supabase=createClient();const {data,error}=await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});if(error)setError(error.message);else if(data.user){await supabase.from('profiles').upsert({id:data.user.id,full_name:name,role:'student'});setDone(true)}setLoading(false)}return <main className="container" style={{maxWidth:560,paddingTop:60}}><Link href="/" className="logo">EduReach <span>Hub</span></Link><div className="card" style={{marginTop:25}}><h1 style={{fontSize:34}}>Create your account</h1><p className="muted">Start with the basics. You can complete your school profile later.</p>{done?<div className="notice">Account created. Check your email if verification is enabled, then <Link href="/login" style={{color:'var(--brand)',fontWeight:700}}>sign in</Link>.</div>:<form className="form" onSubmit={submit}><div className="field"><label>Full name</label><input required value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>Email</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<div className="badge bad">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?'Creating…':'Create account'}</button></form>}<p className="muted">Already registered? <Link href="/login" style={{color:'var(--brand)',fontWeight:700}}>Sign in</Link></p></div></main>}
